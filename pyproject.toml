[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "depth-estimation"
version = "0.1.0"
dependencies = ["numpy", "opencv-python", "pillow", "requests", "ultralytics"]

[tool.setuptools.packages.find]
where = ["yolotest"]
include = ["yolotest*"]
